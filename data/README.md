# Data

Local dataset storage.

Expected layout after running `python scripts/download_data.py`:

```text
data/
├── raw/
│   └── deepglobe-road-extraction-dataset/
├── interim/
└── processed/
```

Only lightweight metadata or placeholders should be committed here. Raw dataset contents should stay untracked.
